$(function () {
    // Code pour centrer les éléments verticalement sur la page home et login
    if ($(".home-page").length > 0) {
        // Si on est sur la page d'accueil
        $("header").addClass("fixed-header");
    }
});